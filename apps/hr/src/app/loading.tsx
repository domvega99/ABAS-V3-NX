import { LinearProgress } from "@mui/material";

const Loading = () =>{
    return(
        <div>
            <LinearProgress sx={{ color: 'primary.dark' }} />
        </div>
    )
}

export default Loading;